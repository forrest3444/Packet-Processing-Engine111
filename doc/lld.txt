PPE Low-Level Design
版本：Draft 0.3
状态：首版 RTL 内部接口、状态转换和逐周期行为已收口

本文档细化doc/hld.txt中确定的模块和流水边界，描述内部数据结构、逻辑接口、状态
转换、索引计算、并发更新及实现注意事项。

本文档不得改变doc/ppe_feature_description.txt中的功能语义，也不得改变doc/hld.txt
中的模块职责和状态所有权。若需要改变上述内容，应先更新对应高层文档。

================================================================================
1. 参数与位宽
================================================================================

  N            = 4
  FE_NUM       = 4
  MAX_DEP      = 7
  ISSUE_WIDTH  = 4
  ROB_DEPTH    = 16
  ROB_ID_W     = 4
  SEQ_W        = 5

  RESULT_BANKS = 8
  PACKET_W     = 128
  DESC_W       = 5

新架构可综合RTL使用SystemVerilog。顶层外部端口保持逐lane独立命名；模块内部重复
lane/FE通路使用packed array、packed struct或interface表达。状态数组只能由一个明确
的always_ff进程拥有，纯组合选择器使用always_comb并提供完整默认赋值。

================================================================================
2. 内部架构图与典型数据流
================================================================================

2.1 Ingress与Scheduler内部架构图

  Top Inputs
  in_valid / in_packet / in_desc
        |
        | Flow D-A：输入接收
        v
  +--------------------------------------------------------------------------+
  |                       ingress / scheduler                                |
  |                                                                          |
  |  +---------------------------+                                           |
  |  | D0 Input Admission        |<------------------------- bkps            |
  |  | - valid rank              |                                           |
  |  | - dense seq tag alloc     |---- alloc metadata -------------------+---|--+
  |  | - target tag/state init   |                                           |  |
  |  +-------------+-------------+                                           |  |
  |                | registered issue entry                                 |  |
  |                v                                                         |  |
  |  +---------------------------+       +-------------------------------+   |  |
  |  | Issue Table [0..15]       |       | Sequence / Control State      |   |  |
  |  | - packet / seq tag        |       | - next_seq_tag                |   |  |
  |  | - target seq tag          |       | - class_rr_ptr / fe_rr_ptr   |   |  |
  |  | - unified state/queued    |       +-------------------------------+   |  |
  |  +------+--------------------+                                           |  |
  |         |                                                                |  |
  |         | Flow D-B：依赖初始化/等待                                     |  |
  |         v                                                                |  |
  |  +---------------------------+       +-------------------------------+      |
  |  | Dependency Event Control  |<----->| Backend Status / Gather       |      |
  |  | - D0 status init          |       | active ROB / history          |      |
  |  | - completion wakeup       |<--- completion seq_tag events ---------------+
  |  | - WAIT_DEP                |       | completion / retire bypass    |      |
  |  +-------------+-------------+
  |                |
  |                | READY discovery / ID enqueue
  |                v
  |  +---------------------------+       +-------------------------------+
  |  | 4 Delay-Class ID FIFOs    |------>| D2 4x4 Legal/FE Selector      |
  |  | - registered head windows |       | - forced-first + rotating RR  |
  |  | - up to 4 IDs per class   |       | - assign up to 4 FEs          |
  |  +-------------+-------------+       +---------------+---------------+
  |                | selected entry indices / FE mapping  |
  |                +----------------------+----------------+
  |                                       v
  |  +-------------------------------------------------------------------+
  |  | D3 Operand Read / FE Issue                                      |
  |  | - read selected packet                                          |
  |  | - four-level authoritative dependency gather ---------+          |
  |  | - packet/dependency to FE routing                      |          |
  |  +-------------------------+-------------------------------|----------+
  |                            |                               |
  |                            | Flow D-C：FE派发              |
  |                            v                               |
  |  +-------------------------------------------------------------------+
  |  | Per-FE State                                                    |
  |  | launch register / 8-slot return calendar and tags               |
  |  +-------------------------------------------------------------------+
  +-----------------------------+--------------------------------------------+
                                |
                                v
                         FE0 / FE1 / FE2 / FE3

  Scheduler与backend之间的跨模块路径：

    a) ingress向backend申请ROB entry并发送seq metadata；
    b) D0读取最多4路依赖available状态，D3读取最多4路权威依赖数据；
    c) backend按completion、retirement、active ROB、history优先级响应；
    d) FE返回事件写active ROB，并以完整tag唤醒scheduler等待项。

2.2 Backend内部架构图

                         From ppe_ingress
                    alloc_valid / seq_tag
                                  |
                                  | Flow R-A：ROB分配
                                  v
  +----------------------------------------------------------------------------+
  |                                backend                                     |
  |                                                                            |
  |  +--------------------------+                                              |
  |  | A0 Allocation Manager    |---- occupancy update                         |
  |  +-------------+------------+                                              |
  |                | metadata writes                                           |
  |                v                                                           |
  |  +-----------------------------------------------------------------------+ |
  |  |                         ROB [0..15]                                  | |
  |  | valid / seq_tag / result_valid / fe_out_data                        | |
  |  +-------+--------------------------+---------------------------+--------+ |
  |          ^                          |                           |          |
  |          |                          |                           |          |
  |          | Flow R-B：FE写回         | Flow R-C：依赖查询        |          |
  |  +-------+------------------+       v                           |          |
  |  | W0 Writeback             |  +--------------------------+    |          |
  |  | - 4-way full-tag check   |  | G0 Dependency Resolve    |    |          |
  |  | - 4-way result write     |  | - completion bypass      |    |          |
  |  +-------+------------------+  | - retirement bypass      |    |          |
  |          ^                     | - active ROB / history    |    |          |
  |          |                     +-------------+------------+    |          |
  |   FE0..3 result                             ^                  |          |
  |   + seq_tag                                 |                  |          |
  |                                             |                  |          |
  |                              status / 4-way data response     |          |
  |                                                                |          |
  |                                                                v          |
  |  +--------------------------+       +----------------------------------+   |
  |  | 8-bank Retired History   |<------| R0 In-order Retire              |   |
  |  | valid / tag / 128b data  | write | - head+0..3 DONE scan           |   |
  |  +-------------+------------+       | - retire count                  |   |
  |                ^                    | - data select / lane rotation    |   |
  |                | dependency read    +----------------+-----------------+   |
  |                +-------------------------------------+                     |
  |                                                      | Flow R-D：退休输出 |
  |                                                      v                     |
  |                                          +-----------------------------+   |
  |                                          | Registered Output Lanes     |   |
  |                                          | out_valid / out_packet      |   |
  |                                          +-----------------------------+   |
  |                                                                            |
  |  +--------------------------+                                              |
  |  | Backpressure Generator   |<--- occupancy / free_count / input_valid     |
  |  +-------------+------------+                                              |
  |                | bkps                                                       |
  +----------------+-----------------------------------------------------------+

  Backend内部四条通路：

    A0：输入分配ROB entry；
    W0：接收最多4路FE乱序完成结果；
    G0：为D0提供available状态并为D3提供最多4路权威依赖数据；
    R0：从ROB head保序退休、更新retired history并产生寄存器输出。

2.3 典型无依赖报文数据流

  1) D0捕获输入并分配seq，Issue Table和A0从seq低位索引本地entry，同时将无依赖报文标记为
     ISSUE_READY；
  3) D2选择报文和目标FE；
  4) D3读取packet并向FE发出请求，issue entry释放；
  5) FE返回，W0按完整seq tag低位写ROB并设置result_valid；
  6) 报文到达ROB head且DONE后，R0将数据装入输出寄存器；
  7) 同一退休沿更新对应retired history bank并释放ROB entry。

2.4 典型依赖等待数据流

  1) D0捕获依赖报文并预计算完整target seq tag，target ROB/history索引由tag低位导出；
  2) D0通过G0查询active ROB/history小位宽状态，并检查同周期completion/retirement
     bypass；ready时进入ISSUE_READY，否则进入ISSUE_WAIT_DEP；
  3) 目标FE返回时，W0写active ROB并以完整tag唤醒ISSUE_WAIT_DEP entry；
  4) 被唤醒报文最早下一周期进入READY并由D2选择；
  5) D3通过G0按四级优先级gather权威依赖数据并向FE派发；
  6) 目标退休前从active ROB读取，退休沿可由retirement bypass读取，退休后从history读取。

2.5 典型保序阻塞数据流

  1) 多个FE乱序返回，W0分别写入对应ROB entry；
  2) 后序ROB entry可以先DONE，但R0只检查ROB head开始的连续DONE项；
  3) head未DONE时retire_count=0，后序DONE结果继续保存在ROB；
  4) head完成后的下一周期，R0一次退休最多4个连续DONE报文；
  5) 输出顺序由ROB head确定，与FE完成顺序无关。

2.6 FE固定延迟流水行为

每个FE每周期最多接收一路请求，允许多个请求同时在途，并允许输入和输出在同一周期
发生。请求在cycle n接收且desc_delay=d时，固定在cycle n+d+1返回。行为模型可用
四个未来输出槽表达：

  slot0：下一周期输出
  slot1：下两周期输出
  slot2：下三周期输出
  slot3：下四周期输出

接收请求时将结果预约到slot[d]，每周期输出当前slot0并将slot1..3向低位推进。
同一FE每个未来周期只有一个输出槽；新请求如果与已有在途请求预约同一输出周期，
属于PPE协议违规。FE无ready或重放能力，正常RTL不得产生这种输入。

FE内部数据算法对PPE保持黑盒。仓库行为模型只需与验证VIP保持一致；PPE功能正确性
依赖的是输入数据、依赖数据和delay被正确送达，以及返回周期和请求关联正确。

================================================================================
3. Ingress与Scheduler内部数据结构
================================================================================

3.1 Issue Entry

Issue table深度为16，以ROB ID作为entry索引，主要数据结构为：

  typedef enum logic [1:0] {
      ISSUE_FREE,
      ISSUE_WAIT_DEP,
      ISSUE_READY,
      ISSUE_SELECTED
  } issue_state_e;

  typedef struct packed {
      issue_state_e        state;
      logic [SEQ_W-1:0]    seq_tag;
      logic [SEQ_W-1:0]    target_seq_tag;
      logic [1:0]          delay;
      logic                dep_required;
      logic                dep_satisfied;
      logic [127:0]        packet;
  } issue_entry_t;

状态与依赖字段必须满足：

  - ISSUE_FREE：entry无效，其余字段不作要求；
  - ISSUE_WAIT_DEP：dep_required=1且dep_satisfied=0；
  - ISSUE_READY：无依赖，或dep_required=1且dep_satisfied=1；
  - ISSUE_SELECTED：已经被ppe_fe_scheduler不可撤销地选中，依赖必定满足。

queued作为独立于issue_entry_t的FIFO成员资格账本，仅表示READY entry的ID已经存在于
唯一一个delay-class FIFO。保留该位是为了支持READY事件超过单周期入队带宽时的后续
发现；它不改变报文功能状态。target ROB ID固定从target_seq_tag低位派生，不单独保存。

3.2 Sequence State

Ingress维护：

  next_seq_tag[4:0]

next_seq_tag按2^SEQ_W回绕。不保存独立residue计数器或entry字段。

3.3 Ready Queue and Arbitration State

Scheduler维护4个按desc_delay分类、每类深度16的READY ID FIFO：

  ready_q_id[class][0..15][3:0]
  ready_q_count[class][4:0]
  enqueue_rr_ptr[3:0]
  class_rr_ptr[1:0]

FIFO只保存issue entry ID，报文、tag和依赖信息仍只在issue table保存。每类
位置0..3构成寄存器化队首窗口；D2可连续消费其中前K项，K为该类本周期grant数，范围
0..4。enqueue_rr_ptr用于公平扫描尚未queued的READY entry，每周期最多发现并入队4项。
class_rr_ptr用于普通D2 grant的分类轮转；成功grant后推进到最后服务分类的下一类。

其中ready_q_id、ready_q_count、entry queued和enqueue_rr_ptr由ppe_issue_table拥有。
class_rr_ptr和fe_rr_ptr由ppe_fe_scheduler保存，并与return calendar预约在同一选择沿更新。

3.4 Per-FE State

四个FE的返回日历在模块内部扁平为32项，索引为{fe_id[1:0], slot[2:0]}。Issue table
为每个FE保存一组selected-tag流水寄存器：

  selected_valid
  selected_seq_tag[4:0]

ppe_fe_scheduler保存共享返回日历：

  return_valid[0:31]
  return_seq_tag[0:31][4:0]

selected-tag寄存器是D2到D3的单级控制流水。D3从tag低位临时派生issue entry索引，
并在每个周期消费当前valid issue bundle
并通过G0读取权威依赖数据。合法选择不可撤销，依赖bundle的权威data_valid必须成立；
D2允许在同一上升沿为该FE写入下一selected tag，不因当前bundle存在而产生隔周期气泡。

return calendar以共享schedule_phase索引。只有valid位参与空闲判断；seq_tag在valid=0时
不要求清零。日历保存已经预约但尚未返回的全部FE请求关联信息，ROB写地址在返回时从
完整tag低位临时派生。

3.5 FE Arbitration State

  fe_rr_ptr[1:0]
  schedule_phase[2:0]

fe_rr_ptr指向下一次FE轮询起点。成功分配后推进到最后一个被分配FE的下一位置。
schedule_phase表示本周期上升沿对应的返回日历槽，每个非复位上升沿加1自然回绕。

若D2选择发生在cycle n上升沿，所选请求将在cycle n+1上升沿由D3送入FE。delay=d
的目标返回沿为cycle n+d+2，因此D2目标槽计算为：

  target_slot = schedule_phase + zero_extend(d) + 3'd2

以上加法按3 bit自然回绕。最长预约距离为5周期，小于8槽深度，不会与未到期预约
发生模回绕混淆。

3.6 Dependency Control State

Scheduler不保存非权威依赖数据副本，也不保存fallback或replay状态。依赖是否可调度
由issue entry的state、dep_required和dep_satisfied共同编码。Completion wakeup只更新
小位宽entry状态；128 bit结果始终由backend active ROB或retired history拥有。

================================================================================
4. Backend内部数据结构
================================================================================

4.1 ROB Entry

ROB深度为16，每个entry至少包含：

  valid
  seq_tag[4:0]
  result_valid / done
  fe_out_data[127:0]

fe_out_data同时作为最终输出数据和依赖结果，不保存两份128 bit数据。

4.2 ROB Global State

  head_ptr[3:0]
  occupancy[4:0]

  free_count = ROB_DEPTH - occupancy
  available_count = free_count + retire_count

head_ptr按16自然回绕。Allocation物理索引由alloc_seq_tag低位直接产生，不保存tail_ptr。
occupancy范围为0..16。

4.3 Retired History Bank

共8个bank。每个bank保存：

  valid
  seq_tag[4:0]
  result_data[127:0]

bank编号直接等于seq_tag[2:0]。不保存独立seq_residue字段；bank命中必须同时检查
valid和完整seq_tag。

4.4 Retire Output State

  start_lane[1:0]
  out_valid0..3
  out_packet0[127:0]
  out_packet1[127:0]
  out_packet2[127:0]
  out_packet3[127:0]

所有out_valid/out_packet必须由寄存器驱动。

================================================================================
5. 模块间逻辑接口
================================================================================

5.0 Top输入与Backend反压需求

ppe_top将原始顶层in_valid0..3分别直接连接到backend容量查询输入。
Backend必须使用未经bkps门控的原始in_valid0..3计算input_count，不得使用alloc_valid
反推input_count，以避免形成bkps组合环。该路径只读取valid，不向backend传递
packet或descriptor。

Top将外部独立lane端口适配为ingress内部packed array：

  in_valid_i[N]
  in_packet_i[N][127:0]
  in_desc_i[N][4:0]
  bkps_i

5.1 Ingress Allocation Bundle

Ingress输出保持原始lane索引，不对128 bit packet做物理压缩：

  alloc_valid_o[N]
  alloc_seq_tag_o[N][SEQ_W-1:0]
  alloc_target_seq_tag_o[N][SEQ_W-1:0]
  alloc_packet_o[N][127:0]
  alloc_delay_o[N][1:0]
  alloc_dep_required_o[N]

Issue table和backend同时捕获该metadata，并分别从alloc_seq_tag低位派生本地entry
索引。不存在allocation ROB ID请求/响应或独立residue接口。
完整desc不跨越ingress边界：desc[1:0]解码为alloc_delay，desc[4:2]是否非零解码为
alloc_dep_required；实际依赖距离已经体现在alloc_target_seq_tag中。

5.2 Allocation Physical Index

完整alloc_seq_tag是allocation的唯一跨模块身份。Issue table和backend在模块内部独立
派生：

  alloc_rob_id = alloc_seq_tag[ROB_ID_W-1:0]

该低位切片只作为本地数组索引，不形成端口或持久metadata字段。首版ROB_DEPTH=16且
seq tag密集分配，因此无需backend向issue table返回allocation ID或accept响应。采用此
映射时要求ISSUE_DEPTH=ROB_DEPTH=2^ROB_ID_W；非2次幂ROB必须先重新定义物理索引规则。

5.3 Issue Table <-> Backend：Dependency Resolve

D0为最多N个新allocation发出小位宽状态请求：

  dep_status_valid[N]
  dep_status_target_seq_tag[N][SEQ_W-1:0]
  dep_status_available[N]

D3为最多ISSUE_WIDTH个已选依赖报文发出数据请求：

  dep_gather_valid[ISSUE_WIDTH]
  dep_gather_target_seq_tag[ISSUE_WIDTH][SEQ_W-1:0]
  dep_gather_data_valid[ISSUE_WIDTH]
  dep_gather_data[ISSUE_WIDTH][127:0]

Backend组合解析每路请求，active ROB和history索引均由target seq tag低位内部派生，
不跨模块传递target ROB ID或residue。D0只使用available；D3使用data_valid和data。
正常状态下ISSUE_SELECTED且dep_required的D3查询必须data_valid，否则属于内部一致性错误，不通过
fallback/replay恢复。

请求无效时响应定义为：

  - dep_status_valid=0时，dep_status_available必须为0；
  - dep_gather_valid=0时，dep_gather_data_valid必须为0；
  - dep_gather_data_valid=0时，dep_gather_data不作功能要求，实现应赋0以抑制无意义切换。

Issue Table直接管理请求资格、completion wakeup比较和D3响应语义适配，不设置中间
dependency模块或额外流水边界。

5.4 Issue Table <-> FE Scheduler

ppe_issue_table接收D0 allocation bundle：

  alloc_valid[N]
  alloc_seq_tag[N][SEQ_W-1:0]
  alloc_target_seq_tag[N][SEQ_W-1:0]
  alloc_packet[N][127:0]
  alloc_delay[N][1:0]
  alloc_dep_required[N]

Issue Table直接形成D0 status query并接收available：

  dep_status_valid[N]
  dep_status_target_seq_tag[N][SEQ_W-1:0]
  dep_status_available[N]

同时接收completion_valid[FE_NUM]和完整completion_seq_tag[FE_NUM]。Completion tag低位
作为扫描ISSUE_WAIT_DEP entry时的小位宽预筛选，
完整target_seq_tag确认wake hit；多个entry允许依赖同一completion，不传递completion
ROB ID。

ppe_issue_table拥有READY发现、queued和delay-class FIFO。接口维度使用独立参数
DELAY_CLASS_NUM和CAND_WINDOW_DEPTH，不借用输入lane数量N：

  candidate_valid[DELAY_CLASS_NUM][CAND_WINDOW_DEPTH]
  candidate_seq_tag[DELAY_CLASS_NUM][CAND_WINDOW_DEPTH][SEQ_W-1:0]

首版DELAY_CLASS_NUM=4、CAND_WINDOW_DEPTH=4。每个candidate_valid对应的entry必须处于
ISSUE_READY，ISSUE_WAIT_DEP和ISSUE_SELECTED entry禁止出现在FIFO或候选窗口。候选接口不携带
packet或target tag。

ppe_fe_scheduler接口为：

  clk
  rst_n

  candidate_valid[DELAY_CLASS_NUM][CAND_WINDOW_DEPTH]
  candidate_seq_tag[DELAY_CLASS_NUM][CAND_WINDOW_DEPTH][SEQ_W-1:0]
  fe_out_valid[FE_NUM]

  select_valid[FE_NUM]
  select_seq_tag[FE_NUM][SEQ_W-1:0]
  completion_valid[FE_NUM]
  completion_seq_tag[FE_NUM][SEQ_W-1:0]

ppe_fe_scheduler内部从schedule_phase和return calendar生成delay-class到FE合法矩阵，
不将fe_legal、RR起点/next-value或calendar槽暴露为端口。select是原子事件：同一内部
决策既输出给Issue Table，又在选择沿写入对应FE的return calendar。不存在独立grant或
reserve握手。模块内部保存class_rr_ptr、fe_rr_ptr、schedule_phase、return_valid和
return_seq_tag，不保存issue entry、READY FIFO、packet或dependency data。

select_valid[fe]=1表示该entry已满足依赖且对应FE返回槽预约已经成功。Issue table在该沿
将entry锁定为ISSUE_SELECTED并保存per-FE selected seq tag；选择不得取消或rollback。下一
周期直接形成按FE编号排列的D3 issue bundle：

  issue_valid[FE_NUM]
  issue_packet[FE_NUM][127:0]
  issue_delay[FE_NUM][1:0]
  issue_dep_required[FE_NUM]
  issue_dep_data[FE_NUM][127:0]

issue_valid=0时其余issue bundle字段输出0。合法选择在下一周期必然进入FE；Issue table
在该FE接收沿自动将对应旧entry转为ISSUE_FREE，不需要外部再次回送物理ID或commit。
同一沿允许ppe_fe_scheduler为该FE锁存下一项选择，从而保持逐周期发射。接口没有ready。

Issue Table仅对issue_valid且issue_dep_required的bundle形成ROB gather请求。合法依赖
issue的gather data_valid必须为1，但它不参与issue_valid或entry释放条件。下游生成：

  fe_dep_valid = issue_valid && issue_dep_required

不设置独立issue_dep_valid端口。

5.5 Scheduler -> FE

每个FE使用一组独立命名端口：

  fe_in_valid0..3
  fe_in_data0..3[127:0]
  fe_dep_valid0..3
  fe_dep_data0..3[127:0]
  fe_desc_delay0..3[1:0]

每个FE每周期最多接受一路fe_in_valid。delay=d的请求固定在接收后的第d+1周期返回。
接口没有ready；scheduler必须通过8槽返回日历保证同一FE的所有在途请求目标输出周期
互不冲突。FE返回同周期允许接收新的无冲突请求。

5.6 FE/Scheduler Tag -> Backend Writeback

ppe_fe_scheduler接收fe_out_valid，并以当前schedule_phase对应calendar槽进行关联：

  completion_valid[FE_NUM]
  completion_seq_tag[FE_NUM][SEQ_W-1:0]

合法状态下：

  completion_valid[fe] = fe_out_valid[fe] && return_valid[fe][schedule_phase]
  completion_seq_tag[fe] = return_seq_tag[fe][schedule_phase]

completion valid/tag同时送Issue Table执行wakeup并送backend执行writeback。每个FE到
backend的数据接口为：

  wb_valid0..3
  wb_seq_tag0..3[4:0]
  wb_data0..3[127:0]

其中wb_valid/wb_seq_tag直接使用对应completion事件，wb_data直接来自FE，不经过
ppe_fe_scheduler。Backend内部以wb_seq_tag低ROB_ID_W位生成本地写地址。

5.7 ROB -> Retire Output -> Top

ppe_rob向ppe_retire_output提供：

  retire_valid[N]
  retire_data[N][127:0]

ppe_rob不输出物理lane编号或start_lane。ppe_retire_output不向ppe_rob返回ready、accept
或其他流控信号。

对外接口分别由对应状态所有者驱动：

  ppe_rob          -> bkps
  ppe_retire_output -> out_valid0..3
  ppe_retire_output -> out_packet0..3[127:0]

5.8 Top -> PPE子模块/FE：Internal Reset

ppe_top使用外部rst_n实现唯一的两级复位同步器，生成internal_rst_n。
internal_rst_n异步拉低、同步释放，并作为ingress、scheduler、ppe_rob、
ppe_retire_output和四个FE
的共享低有效复位。各子模块不得再次同步复位或产生独立释放时序。

================================================================================
6. Ingress D0：输入接收与分配
================================================================================

6.1 Input Rank

对于lane i：

  lane_rank[i] = 当前周期lane编号小于i且valid=1的lane数量

当bkps=0时：

  seq_tag[i]     = next_seq_tag + lane_rank[i]
  alloc_valid[i] = in_valid[i]

当bkps=1或internal_rst_n=0时，所有alloc_valid必须为0，issue table和ROB不写新entry。

seq_tag使用固定位宽自然回绕。Ingress不读取ROB指针，也不产生rob_id或residue输出；
backend和issue table之间不存在ROB ID通路。

6.2 Dependency Input Contract

输入协议保证非零dep_offset不大于本次复位后排在当前报文之前的报文数量，其中包括
同周期lane编号更小且同时被接收的报文。Ingress不保存历史计数器，不检测或修改
非法dep_offset。desc_delay始终保持输入desc[1:0]不变。

6.3 Dependency Target Precompute

若dep_offset=K且K不为0：

  target_seq_tag = seq_tag - K mod 32

密集seq分配建立并必须保持以下不变量：

  rob_id         = seq_tag[3:0]
  target_rob_id  = target_seq_tag[3:0]
  history_bank   = target_seq_tag[2:0]

ROB和history索引均由完整tag低位组合派生，不作为ingress输出或entry存储字段。

6.4 D0 Register Update

接收沿同时完成：

  - issue table使用alloc_seq_tag低位和ingress metadata写entry；
  - ingress向backend提交alloc_valid和alloc_seq_tag；
  - 推进next_seq_tag。

输入被捕获后最早下一周期进入D2，不允许组合直通FE。

================================================================================
7. Scheduler依赖初始化与事件唤醒
================================================================================

7.1 D0 New Entry Initialization

  dep_offset=0：dep_required <- 0, dep_satisfied <- 1, state <- ISSUE_READY

  dep_offset!=0：向G0发送完整target_seq_tag。G0内部派生target ROB/history索引，并按
  completion bypass、retirement bypass、active ROB和retired history顺序返回ready。
  ready时dep_required <- 1, dep_satisfied <- 1, state <- ISSUE_READY；否则
  dep_required <- 1, dep_satisfied <- 0, state <- ISSUE_WAIT_DEP。该组合状态查询不进入
  bkps路径，不返回或复制128 bit依赖数据。

7.2 Writeback Wakeup

Issue Table对每个ISSUE_WAIT_DEP entry先比较target和completion的低位，再比较完整tag：

  wake_hit = wb_valid
          && target_seq_tag[ROB_ID_W-1:0] == wb_seq_tag[ROB_ID_W-1:0]
          && target_seq_tag == wb_seq_tag

任意wake_hit成立时：

  dep_satisfied <- 1
  state <- ISSUE_READY

比较顺序建议先rob_id后seq_tag，以减少完整tag比较的无效翻转。

Issue Table据此只更新小位宽状态，不复制128 bit结果。被唤醒entry可在
该沿进入分类FIFO，但最早下一周期参与D2，不允许completion组合直通FE输入。

7.3 Simultaneous Initialization and Completion

D0初始化的completion bypass与ISSUE_WAIT_DEP wakeup使用相同的完整tag比较。本周期
新分配entry若目标在同周期completion中命中，则沿后直接进入ISSUE_READY；该entry仍最早从
下一周期参与D2。若目标active ROB entry存在但result_valid=0且无completion bypass，
G0必须返回not ready，禁止继续使用history。

Completion wakeup首版不增加寄存级。若completion与allocation作用于同一物理entry，
allocation对沿后issue_entry和queued具有最终优先权；完整seq_tag防止旧身份事件唤醒
新entry。未来只有在实现时序证据表明广播比较路径不满足要求时，才允许同步修改HLD/LLD
后增加流水边界。

================================================================================
8. Scheduler D2：报文与FE仲裁
================================================================================

8.1 READY Discovery and Classification

ISSUE_READY且queued=0的entry可被发现。发现逻辑从enqueue_rr_ptr开始公平
扫描16项，每周期最多选择4项；选择后按desc_delay写入对应分类FIFO尾部并置queued=1。
扫描到的既有READY entry和本周期completion命中的等待项优先于D0新分配，防止
持续输入使唤醒积压饥饿。命中的等待项在状态转为READY的同一沿追加到分类FIFO；若仍
有入队带宽，D0已经确认依赖满足的新分配也在捕获沿追加，使两类报文下一周期可参与
D2。若同周期候选量超过4，未入队entry保持或转为READY且queued=0，后续周期继续发现。

ISSUE_WAIT_DEP不进入分类FIFO。所有FIFO有效ID必须对应ISSUE_READY entry。Completion
唤醒不组合直通D2，但允许在唤醒沿入队并从
下一周期参与匹配。全模块最多16个有效entry且每个entry最多入队一次，故任一深度16
分类FIFO不会溢出。

8.2 Head Windows and Legal Matrix

每类FIFO前min(count,4)个ID构成队首窗口。该类与FE的legal条件为该类至少有一个队首
候选，且：

  target_slot = schedule_phase + zero_extend(class) + 3'd2
  legal[class][fe] = !return_valid[fe][target_slot]

同一类可以在本周期获得多个FE，依次消费窗口中的第0、第1、第2、第3项；不得将4x4
legal matrix解释为每行至多一个匹配。一个FE每周期仍最多接收一个D2分配。

8.3 Forced-First and Rotating Selection

普通候选的第一个grant从各非空且至少存在一个legal FE的分类队首中选择全局最老
seq_tag；5 bit序号差在最多16个在途报文
范围内比较。余下grant从class_rr_ptr开始轮转分类，并优先服务仅剩一个可用FE的受约束
分类。为分类选择FE时，从fe_rr_ptr开始打破平局，并优先保留被更多其他分类需要的FE。

以上规则必须保证持续可调度分类最终获得服务。同一分类每获得一个grant，其下一个
队首ID立即成为该类后续grant候选，直到本周期已取4项或无空闲legal FE。

8.4 FE Selection and Return Reservation

对候选报文计算：

  target_slot = schedule_phase + zero_extend(desc_delay) + 3'd2
  fe_available = !return_valid[fe_id][target_slot]

一个FE每周期最多接收一个D2分配。分配成功后：

  - Issue table保存该FE的selected valid和完整seq_tag；
  - return calendar[target_slot]写入valid和完整seq_tag；
  - issue entry进入ISSUE_SELECTED。

若某候选无法匹配任何FE，则保持ISSUE_READY并等待后续周期，不得因其他候选成功而丢失。
一旦select_valid产生，选择不可取消或rollback；ppe_fe_scheduler必须在产生选择前确认依赖已经
满足且目标FE返回槽预约可用。
D2不得将FE存在其他在途请求作为不可用条件。

8.5 Queue and RR Update

每类FIFO同周期先移除已grant的连续队首ID，再在剩余尾部追加本周期新发现的该类ID；
同一entry被grant时清queued。若同址issue entry在该沿又被D0重新分配，D0初始化
state和queued具有最终优先级。至少一个普通grant成功时，class_rr_ptr推进到最后服务
分类的下一类；至少一个FE分配成功时，fe_rr_ptr推进到最后分配FE的下一位置。

================================================================================
9. Scheduler D3：Operand Read与FE Issue
================================================================================

对每个issue_valid=1的FE：

  - Issue table从内部selected seq tag低位索引packet、delay和dep_required；
  - dep_required=0时输出fe_dep_valid=0、fe_dep_data=0；
  - dep_required=1时向G0发送完整target_seq_tag；
  - G0按completion bypass、retirement bypass、active ROB、retired history顺序解析；
  - 将packet、dependency和delay路由到目标FE；有依赖时gather data_valid必须为1；
  - 拉高对应FE的fe_in_valid一个周期。

正常请求在FE接收沿：

  - issue entry转为ISSUE_FREE；
  - 保留D2已经写入的return calendar预约；
  - 当前selected tag被同周期D2结果替换，若无新选择则清除selected_valid。

D3正常路径包含16:1 packet mux、最多4路active ROB/history 128 bit读取、两级bypass
比较/选择和FE数据路由，是首版重点关注的宽数据路径。依赖entry进入ISSUE_SELECTED后，
其目标结果不会失效：目标退休前由active ROB提供，退休沿由retirement bypass提供，
退休后由history提供。若dep_gather_data_valid=0，属于内部一致性错误；验证侧必须报错，
该情况属于验证必须捕获的内部一致性错误，设计不设置可综合rollback、fallback或replay状态。

================================================================================
10. Backend A0：ROB Allocation
================================================================================

对于本周期每个alloc_valid：

  alloc_rob_id = alloc_seq_tag[ROB_ID_W-1:0]
  rob[alloc_rob_id].valid          <- 1
  rob[alloc_rob_id].seq_tag        <- alloc_seq_tag
  rob[alloc_rob_id].result_valid   <- 0

A0不写fe_out_data。无效entry的数据内容不要求清零。

================================================================================
11. Backend G0：依赖状态与数据解析
================================================================================

11.1 Common Resolve Rule

给定target_seq_tag，先在backend内部派生：

  target_rob_id = target_seq_tag[ROB_ID_W-1:0]
  history_bank  = target_seq_tag[2:0]

然后每路请求按以下顺序解析：

  1) 若任一wb_commit的完整seq_tag匹配：
       satisfied = 1, source = COMPLETION_BYPASS, data = wb_data
  2) 否则若任一retire entry的完整seq_tag匹配：
       satisfied = 1, source = RETIREMENT_BYPASS, data = 沿前rob_data
  3) 否则若rob[target_rob_id].valid且完整seq_tag匹配：
       result_valid=1时satisfied = 1, source = ACTIVE_ROB；
       result_valid=0时satisfied = 0，并立即停止，禁止查询history
  4) 否则以target_seq_tag[2:0]索引history：
       history.valid且完整seq_tag匹配时satisfied = 1, source = HISTORY；
       否则satisfied = 0

完整seq_tag匹配是必需条件，ROB ID或history bank低3位单独匹配不得视为命中。

11.2 Query Width

D0每周期最多提交N路status query，只返回dep_status_available，不要求切换128 bit数据。
D3每周期最多提交ISSUE_WIDTH路data query，每路返回dep_gather_data_valid和128 bit
data。同一target被多路
请求命中时允许共享一次读取并广播；实现也可描述为等价的独立组合读取。

11.3 Cycle Boundary Rules

Completion bypass可以使D0新entry或ISSUE_WAIT_DEP entry在W0写ROB的同一沿得到available状态，
但该entry最早下一周期参加D2，不允许同沿completion-to-FE直通。Retirement bypass
读取沿前旧ROB数据，并优先于沿后可能复用同一ROB ID的新allocation。上升沿后，目标
改由已更新history命中。该规则不依赖存储体未定义的read-during-write行为。

================================================================================
12. Backend W0：FE Writeback
================================================================================

每个wb_valid先计算轻量写保护：

  wb_rob_id = wb_seq_tag[ROB_ID_W-1:0]
  wb_commit = wb_valid
           && rob[wb_rob_id].valid
           && rob[wb_rob_id].seq_tag == wb_seq_tag
           && !rob[wb_rob_id].result_valid

仅当wb_commit=1时执行：

  rob[wb_rob_id].fe_out_data  <- wb_data
  rob[wb_rob_id].result_valid <- 1

同周期最多4个有效writeback seq tag的低ROB_ID_W位必须互不相同。验证侧必须用SystemVerilog assertion
检查FE返回与当前日历槽valid一致、ROB valid、完整tag、未重复完成和同周期ID互异。
任何检查失败都是
验证错误，不设置可综合sticky异常状态，不参与bkps、派发或退休控制。

W0必须捕获全部符合协议的有效FE返回，不允许通过ready反压FE。首版物理实现必须支持
4路任意地址并发写回，不能使用单写端口存储后串行处理。

每个wb_commit同时形成completion bypass和scheduler wakeup事件，但不写retired
history。事件携带valid、完整seq_tag和128 bit data；本地ROB ID从tag低位派生，wakeup
只使用小位宽字段。

在cycle n上升沿，wb关联信息读取：

  slot = schedule_phase
  wb_valid   = fe_out_valid && return_valid[fe_id][slot]
  wb_seq_tag = return_seq_tag[fe_id][slot]

该沿无条件清除return_valid[fe_id][slot]，并将schedule_phase加1。合法协议下
fe_out_valid必须与对应return_valid相等。FE返回、D3发射和D2预约允许同沿发生；D2
目标槽至少在两周期以后，因此不会与本周期清除槽冲突。

================================================================================
13. Backend R0：保序退休与输出
================================================================================

13.1 Consecutive DONE Scan

从head_ptr开始检查最多4项：

  done0 = rob[head+0].valid && rob[head+0].result_valid
  done1 = done0 && rob[head+1].valid && rob[head+1].result_valid
  done2 = done1 && rob[head+2].valid && rob[head+2].result_valid
  done3 = done2 && rob[head+3].valid && rob[head+3].result_valid

  retire_count = done0 + done1 + done2 + done3

所有ROB索引按16自然回绕。

13.2 ROB Logical Retirement Bundle

ppe_rob输出按报文序排列的逻辑退休bundle：

  retire_valid[N]
  retire_data[N][127:0]

retire_valid从slot 0开始连续有效，不得留洞；slot j对应head+j的沿前ROB结果。
该bundle为组合退休事件，供ppe_retire_output在同一上升沿捕获，不使用ready或反压。

13.3 Retirement Register Update

退休沿在ppe_rob内同时执行：

  - 对每个退休报文写history[rob_seq_tag[2:0]]，保存完整seq_tag和result_data；
  - 清除对应ROB valid/result_valid；
  - head_ptr推进retire_count；
  - occupancy减retire_count并合并同周期allocation数量。

同一退休沿在ppe_retire_output内同时执行：

  - 先将全4路out_valid的next value默认为0，再按start_lane映射K个退休slot；
  - 仅更新本周期out_valid=1的out_packet寄存器，其余out_packet保持；
  - start_lane推进retire_count。

第j个逻辑退休slot映射到lane=(start_lane+j) mod 4，使用沿前start_lane。ROB状态
释放、history更新、输出寄存器装载和start_lane更新属于同一架构退休沿，不新增
流水级或跨模块握手。

当前N=4时，同周期退休的连续报文映射到不同history bank，不存在同bank多写。
未来N=8时，连续8个报文也分别映射到8个不同bank。

13.4 Writeback Visibility

W0在本周期刚写回的entry不参与同周期R0退休，最早下一周期被DONE scan看到。

================================================================================
14. 全局反压
================================================================================

  input_count = popcount(in_valid[3:0])
  free_count  = ROB_DEPTH - occupancy
  available_count = free_count + retire_count

  bkps = !internal_rst_n || (available_count < input_count)

free_count使用本周期上升沿之前的状态。retire_count使用第13.1节由沿前ROB状态形成的
连续DONE前缀，因此available_count包含本周期上升沿将释放的空间。该控制路径允许满
ROB时在有足够退休数量的周期继续接收，但不允许使用W0同周期刚写回形成的退休空间。
复位期间bkps强制为1，不受occupancy和input_count影响。

ROB_DEPTH=16且issue table以seq_tag低位索引。ROB entry能够退休意味着对应issue entry
此前已经派发并释放，因此backend释放的物理槽在同沿也可由相同低位的新allocation复用。

================================================================================
15. 同周期并发更新
================================================================================

A0、G0、W0和R0允许同周期作用于不同entry。W0不写history；R0是history唯一写入
通路。G0的completion和retirement bypass读取本周期事件数据，不写任何权威存储。

必须满足：

  - 新allocation entry允许与本周期退休entry重叠，且仅能复用退休前缀释放的entry；
  - 重叠entry的退休输出、retirement bypass和history写入读取沿前旧内容，allocation
    对沿后ROB valid、seq_tag和result_valid具有最终优先权；
  - 新writeback entry不在同周期退休；
  - 已DONE退休entry不再次收到writeback；
  - 同周期4路writeback ID互不相同；
  - 同周期allocation和retirement数量统一合并更新occupancy；
  - ROB valid、result_valid、head和occupancy只有ppe_rob一个时序状态所有者；
  - start_lane和out_valid/out_packet只有ppe_retire_output一个时序状态所有者。

必须按统一next-state计算：

  occupancy_next = occupancy + alloc_count - retire_count

valid/result_valid数组在同一个时序always block中按writeback、retire、allocation
顺序更新，使同址allocation最后赋值。退休数据选择仍由沿前数组组合产生，不受同沿
allocation非阻塞赋值影响。禁止多个always block驱动同一状态。

================================================================================
16. 复位
================================================================================

ppe_top是复位同步器的唯一所有者。外部rst_n异步拉低同步器两级寄存器，外部
rst_n拉高后经过两个clk上升沿释放internal_rst_n。PPE全部子模块和FE使用同一个
internal_rst_n。

复位清除：

  - issue table unified state；
  - ready_q_count、entry queued、enqueue_rr_ptr、class_rr_ptr和fe_rr_ptr；
  - per-FE selected valid和return calendar valid；
  - schedule_phase；
  - ROB valid/result_valid/head/occupancy；
  - retired history valid；
  - next_seq_tag；
  - ppe_retire_output中的start_lane和out_valid。

无效状态下不要求清零宽数据数组。复位期间bkps=1，所有fe_in_valid和out_valid=0。

================================================================================
17. 初始实现风险与后续观察项
================================================================================

以下项目不改变首版功能和流水边界，实现后根据编译、仿真和后续时序数据观察：

  1) READY发现、分类FIFO更新、D2 4x4 legal matrix和multi-grant组合深度；
  2) D3 packet、4路active ROB/history dependency和FE routing的组合深度；
  3) completion/retirement bypass tag比较、优先mux和宽数据扇出；
  4) 4路任意地址ROB writeback的decode和宽数据扇出；
  5) R0连续DONE scan、数据mux和输出旋转的组合深度。

若上述任一项需要改变流水级、pending storage、仲裁语义或查询端口，必须先更新
HLD和LLD，不属于首版RTL编码时的自由选择。

================================================================================
18. LLD不变量
================================================================================

  1) 同一issue entry不得被重复选择或重复派发。
  2) selected entry、FE launch和return calendar预约必须成对建立。
  3) 有效return calendar槽的seq_tag在返回前不得被覆盖。
  4) 依赖未满足时不得被select；ISSUE_SELECTED依赖entry的权威gather必须data_valid。
  5) 依赖查询必须完整匹配seq_tag。
  6) FE返回只设置active ROB result并形成completion事件，不直接写retired history。
  7) Active ROB tag命中但result_valid=0时必须等待，禁止继续查询history。
  8) 退休只从ROB head开始并且只退休连续DONE entry。
  9) 输出寄存器装载、ROB释放和history更新属于同一退休事件。
  10) ROB occupancy必须等于有效ROB entry数量。
  11) 无效宽数据内容不影响任何功能判断。
  12) 同一FE的有效return calendar槽唯一对应一个目标返回周期，D2不得覆盖有效槽。
  13) 完整seq_tag是唯一跨模块报文身份；allocation、selection、writeback及dependency
      的本地ROB索引必须由对应完整tag低ROB_ID_W位临时导出，不设置ROB ID端口，也不在
      selected pipeline或return calendar中重复保存ID。Issue Table内部ID-only READY FIFO
      仍按第3.3节保存物理索引。
  14) queued=1的entry必须处于READY状态并在唯一一个delay分类FIFO中恰好出现一次；
      D2每类只能消费该FIFO的连续队首，等待依赖entry不得进入FIFO。
